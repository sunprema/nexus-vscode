# Only the two bundles ship: esbuild inlines every dependency into them,
# so node_modules is excluded outright and nothing has to be listed back in.
.vscode/**
src/**
out/**
esbuild.mjs
node_modules/**
.gitignore
tsconfig.json
**/*.map
**/.eslintrc.json
.nexus/**
.nexusignore
.github/**
